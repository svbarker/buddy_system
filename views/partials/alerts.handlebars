{{#if messages.danger}}
	{{#each messages.danger as |message|}}
		<div class="alert alert-warning alert-dismissable">
			{{message}} 
			<button type="button" class="close" data-dismiss="alert">
				&times;
			</button>
		</div>
	{{/each}}
{{/if}}